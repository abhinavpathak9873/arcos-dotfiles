# Personal Sway/SwayFX overrides belong here. The ArcOS system module includes
# this directory after its defaults, so entries below win without editing the
# immutable Nix store.

# Example:
# output DP-1 mode 2560x1440@165Hz adaptive_sync on
# workspace 1 output DP-1
