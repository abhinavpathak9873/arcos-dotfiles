# Abhinav's Kitty defaults

This is the Kitty setup imported from the development workstation on
2026-08-09. ArcOS installs it as the system default while leaving each user
free to override it under `~/.config/kitty`.

The Catppuccin Frappé palette is MIT-licensed by Catppuccin. `abhi-look.conf`
and the tab bindings are Abhinav's local customizations.
